from django.contrib import admin
from .models import Users, Address
from .models import Products, Photos
from .models import Setting, Country, City
from .models import Orders, Offers

# Register your models here.
